{
  "_doc": "Quick PR-shipping check variant of il.json: 1 epoch on a small subsample (~2 min) to confirm a code change did not break the IL (base supervised training) pipeline. The default il.json is the real multi-epoch training config.",
  "train_epochs": 1,
  "warm_up_epoch": 0,
  "batch_size": 8,
  "use_data_augment": true,
  "augment_prob": 0.5,
  "n_train_cap": 400,
  "n_val_cap": 100
}
